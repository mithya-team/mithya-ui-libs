# AGENTS.md

Lib repo for Mithya web + native primitives and registry JSON.

Parent Cursor workspace: `mithya-design-dev-workflow` (sibling `mithya-pilot-client`).

- Component work: `.cursor/skills/ui-lib-component/SKILL.md`
- Contract: `docs/working-model.md`
- Do not edit `mithya-pilot-client` copies from this repo except via registry JSON.

```bash
pnpm install
pnpm build:registry
pnpm serve
```
